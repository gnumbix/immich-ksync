# Build products
/build/
/build-*/
/dist/
/snapshots/
compile_commands.json
.cache/

# Crash reports dropped here for investigation
/.crash/

# Editors
.idea/
.vscode/
*.swp
*~

# Test artefacts. The certificate fixtures themselves are committed on purpose, so the
# hermetic suite needs no openssl at build time; only the intermediates are ignored.
/tests/fixtures/*.csr
/tests/fixtures/*.srl
